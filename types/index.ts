import { IconType } from 'react-icons';

export interface Feature {
  name: string;
  description: string;
  icon: IconType;
}

export interface UseCase {
  name: string;
  description: string;
  icon: IconType;
}
